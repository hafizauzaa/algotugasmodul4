annotation.processing.enabled=true^M
annotation.processing.enabled.in.editor=false^M
annotation.processing.processor.options=
annotation.processing.processors.list=
annotation.processing.run.all.processors=true^M
annotation.processing.source.output=${build.generated.sources.dir}/ap-source-output^M
build.classes.dir=${build.dir}/classes^M
build.classes.excludes=**/*.java,**/*.form^M
# This directory is removed when the project is cleaned:
build.dir=build^M
build.generated.dir=${build.dir}/generated^M
build.generated.sources.dir=${build.dir}/generated-sources^M
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore^M
build.test.classes.dir=${build.dir}/test/classes^M
build.test.results.dir=${build.dir}/test/results^M
# Uncomment to specify the preferred debugger connection transport:
#debug.transport=dt_socket
debug.classpath=\^M
    ${run.classpath}
debug.test.classpath=\^M
    ${run.test.classpath}
# Files in build.classes.dir which should be excluded from distribution jar
dist.archive.excludes=
# This directory is removed when the project is cleaned:
dist.dir=dist^M
dist.jar=${dist.dir}/tugasmodul4.jar^M
dist.javadoc.dir=${dist.dir}/javadoc^M
excludes=
includes=**^M
jar.compress=false^M
javac.classpath=
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false^M
javac.external.vm=true^M
javac.processorpath=\^M
    ${javac.classpath}
javac.source=1.8^M
javac.target=1.8^M
javac.test.classpath=\^M
    ${javac.classpath}:\^M
    ${build.classes.dir}
javac.test.processorpath=\^M
    ${javac.test.classpath}
javadoc.additionalparam=
javadoc.author=false^M
javadoc.encoding=${source.encoding}^M
javadoc.noindex=false^M
javadoc.nonavbar=false^M
javadoc.notree=false^M
javadoc.private=false^M
javadoc.splitindex=true^M
javadoc.use=true^M
javadoc.version=false^M
javadoc.windowtitle=
main.class=
manifest.file=manifest.mf^M
meta.inf.dir=${src.dir}/META-INF^M
mkdist.disabled=false^M
platform.active=default_platform^M
run.classpath=\^M
    ${javac.classpath}:\^M
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project.
# You may also define separate properties like run-sys-prop.name=value instead of -Dname=value.
# To set system properties for unit tests define test-sys-prop.name=value:
run.jvmargs=
run.test.classpath=\^
    ${javac.test.classpath}:\^
    ${build.test.classes.dir}
source.encoding=UTF-8^M
src.dir=src^M
test.src.dir=tes^M
